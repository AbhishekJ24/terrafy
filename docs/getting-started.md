## 🛠️ Getting Started

```bash
# Install dependencies
npm install

# Start the frontend app
npx nx serve frontend

# (Optional) Set up Python env for backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
